<div class="container">
    <div class="modal fade modal-dialog modal-dialog-centered" id="AddModal" tabindex="-1" aria-labelledby="dbModalLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <a type="button" class="btn btn-primary" href="@Url.Action("CreateAuto","Admin")">Auto</a>
                    <a type="button" class="btn btn-primary" href="@Url.Action("CreateMarca","Admin")">Marca</a>
                    <a type="button" class="btn btn-primary" href="@Url.Action("CreateModello","Admin")">Modello</a>
                    <a type="button" class="btn btn-primary" href="@Url.Action("CreateTipo","Admin")">Tipo</a>
                </div>
            </div>
        </div>
    </div>

    <div class="modal fade modal-dialog modal-dialog-centered" id="UpdateModal" tabindex="-1" aria-labelledby="dbModalLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <a type="button" class="btn btn-primary" href="@Url.Action("CreateAuto","Admin")">Auto</a>
                    <a type="button" class="btn btn-primary" href="@Url.Action("CreateMarca","Admin")">Marca</a>
                    <a type="button" class="btn btn-primary" href="@Url.Action("CreateModello","Admin")">Modello</a>
                    <a type="button" class="btn btn-primary" href="@Url.Action("CreateTipo","Admin")">Tipo</a>
                </div>
            </div>
        </div>
    </div>
</div>
<div class="container my-5">
    <div class="row d-flex">
        <div class="col-12 d-flex justify-content-center">
            <a href="@Url.Action("CreateAuto","Admin")" data-bs-toggle="modal" data-bs-target="#AddModal" class="btn btn-primary my-2 mx-2 col-3 me-2"> Aggiungi</a>
            <a href="@Url.Action("Update","Admin")" data-bs-toggle="modal" data-bs-target="#UpdateModal" class="btn btn-primary my-2 mx-2 col-3 me-2"> Aggiorna</a>
        </div>
        <div class="col-12 d-flex justify-content-center">
            <button class="btn btn-primary my-2 mx-2 col-3 me-2">
                Acquisisci macchine
            </button>
        </div>
        <div class="col-12 d-flex justify-content-center">
            <a href="@Url.Action("ListaMacchineAdmin","Admin")" class="btn btn-danger my-2 mx-2 col-3 me-2"> Rimuovi/Acquista</a>
        </div>
    </div>
</div>


